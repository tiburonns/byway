# Release artifacts

The IPA and setup archive are published as assets of the matching GitHub release instead of being committed to Git history.

El IPA y el paquete de configuración se publican como archivos de la versión correspondiente de GitHub, en lugar de almacenarse en el historial de Git.
